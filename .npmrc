# Some dependencies (e.g. react-tilt@0.1.4) declare stale React 15/16 peer
# ranges. They work fine with React 18 in practice, but npm 7+ enforces peer
# ranges strictly and refuses to install. This flag opts back into the npm 6
# behaviour so plain `npm install` works for everyone, including Netlify CI.
legacy-peer-deps=true
